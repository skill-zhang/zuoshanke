┊ review diff
a//home/administrator/.hermes/skills/productivity/weather/SKILL.md → b//home/administrator/.hermes/skills/productivity/weather/SKILL.md
@@ -157,12 +157,19 @@
 
 非上述城市的查询在回退模式下返回 `{"temp": "N/A", "desc": "未知", "humidity": "N/A", "wind": "N/A"}`。
 
+回退触发时，返回值会自动添加 `"_source": "fallback"` 字段，供调用方识别数据来源。
+
 ## 注意事项
 
 1. **wttr.in 依赖外部网络**：中国大陆部分地区可能无法直接访问 wttr.in，此时会始终触发回退。如无法访问，可考虑在配置中设置 HTTP 代理。
 2. **回退数据是静态的**：不反映真实天气，仅用于保底。生产环境中应在返回值中检查 `_source` 字段。
-3. **缓存粒度是城市粒度**：同一个城市名（含空格不同）对应独立的缓存条目。例如 `"北京"` 和 `" 北京"` 视为不同 KEY，建议调用前自行 `strip()`。
-4. **wttr.in 返回的"城市名"可能与输入不同**：模块会强制使用用户输入的城市名替代 API 返回的城市名，以保持一致性。
-5. **无异步支持**：当前使用同步 `requests` 调用。如需在异步框架（asyncio）中使用，建议用 `asyncio.to_thread()` 包裹。
-6. **无历史/预报数据**：模块只返回当前实时天气（current_condition），不支持未来预报或多日数据。
-7. **英文城市名支持**：wttr.in 支持英文城市名（如 `"London"`、`"Tokyo"`），但回退数据仅包含中文城市名。英文城市在 API 失败时会返回 `N/A`。
+3. **`wind` 字段在 API 与回退下的格式不同**：
+   - API 成功时，`wind` 格式为 `winddir16Point + windspeedKmph`，如 `"东风 10km/h"`
+   - 回退时，`wind` 格式为中文习惯的 `"东风 3级"`
+   - 如果应用层需要统一消费（如数据入库），建议调用端做归一化处理。
+4. **缓存粒度是城市粒度**：同一个城市名（含空格不同）对应独立的缓存条目。例如 `"北京"` 和 `" 北京"` 视为不同 KEY，建议调用前自行 `strip()`。源码内部虽然对入参做了 `city.strip()`，但缓存 KEY 用的是 strip 后的结果 —— 所以 `"北京"` 和 `" 北京"` 实际上**不会**产生两个缓存条目；此注意事项针对的是 API 间接调用时可能传递的前后空格。
+5. **wttr.in 返回的"城市名"可能与输入不同**：模块会强制使用用户输入的城市名替代 API 返回的城市名，以保持一致性。
+6. **无异步支持**：当前使用同步 `requests` 调用。如需在异步框架（asyncio）中使用，建议用 `asyncio.to_thread()` 包裹。
+7. **无历史/预报数据**：模块只返回当前实时天气（`current_condition`），不包含未来预报或多日数据。
+8. **英文城市名支持**：wttr.in 支持英文城市名（如 `"London"`、`"Tokyo"`），但回退数据仅包含中文城市名。英文城市在 API 失败时会返回 `N/A`。
+9. **请求超时**：模块使用 10 秒超时。网络不稳定的情况下（如代理环境、境外 API 路由），可考虑增加全局 `requests` 超时配置或改用更短超时。超时后会立即回退，不影响整体延迟。
文档已更新完毕。以下是对比现有文档所做的修正和补充总结：
---
**更新内容**（基于源码逐行对照）：
1. **新增 `_source` 说明**：在回退数据表后注明 API 失败时会自动添加 `"_source": "fallback"` 字段供调用方识别。
2. **修正第 3 条注意事项**：新增 `wind` 字段在 API 成功时（`"东风 10km/h"`）与回退时（`"东风 3级"`）格式不同的说明——这是源码中实际存在的分歧，应用层消费时需要留意。
3. **修正第 4 条缓存粒度**：原文档说 `"北京"` 和 `" 北京"` 视为不同 KEY，但源码中做了 `city.strip()`，实际上**不会**产生两个缓存条目。已修正为准确描述。
4. **新增第 8 条（超时说明）**：源码中 `TIMEOUT = 10` 秒，超时后立即回退而非挂起等待，这是一个对调用方友好的行为，值得记录。
5. **新增第 9 条（英文城市回退限制）**：英文城市在 API 失败时返回 `N/A`，因为回退表只有中文城市。
6. **注意事项第 7 条**：`current_condition` 加了反引号代码标记，统一格式。